- content_for :header do
  = render template: 'ok2/layouts/items/header', locals: { current_section: :main, search_block: false }

- content_for :footer do
  = render template: 'ok2/layouts/items/footer'

= render partial: 'notifications/flash', locals: { format: :json }

= render partial: 'devise/sessions/social_login',       locals: { visible: true  }
= render partial: 'devise/sessions/email_login',        locals: { visible: true  }
= render partial: 'devise/sessions/onetime_login_link', locals: { visible: false }
= render partial: 'devise/sessions/email_singup',       locals: { visible: false }
